import math


def cubed(x):
    """this function returns back number in the 3rd power
    Args:
        x (int,float): any number
    Returns:
        float: this number in the 3rd power
    """
    x=float(x)
    return math.pow(x, 3)
